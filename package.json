{
  "name": "weather-god",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "engines": { "node": ">=20.0.0" },
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -F @apps/web dev",
    "lint": "pnpm -r lint",
    "test": "pnpm -r test"
  }
}
